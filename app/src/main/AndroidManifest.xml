<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for Gallery Access -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RideQuest"
        tools:targetApi="31">

        <!-- Landing/Onboarding -->
        <activity
            android:name=".LandingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Authentication -->
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />

        <!-- Main App -->
        <activity android:name=".MainActivity" />
        <activity android:name=".CarDetailActivity" />
        <activity android:name=".BookingActivity" />
        <activity android:name=".PaymentActivity" />

        <!-- Profile -->
        <activity android:name=".ProfileActivity" />
        <activity android:name=".EditProfileActivity" />

        <!-- Bookings -->
        <activity
            android:name=".MyBookingsActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".MaintenanceDetailsActivity"
            android:exported="false"/>

        <!-- Admin -->
        <activity android:name=".AdminDashboardActivity" />
        <activity android:name=".AddVehicleActivity" />
        <activity android:name=".EditVehicleActivity" />
        <activity android:name=".BookingDetailsActivity" />
        <activity android:name=".MaintenanceDashboardActivity" />
        <activity android:name=".MaintenanceActivity" />
        <activity android:name=".InspectionDashboardActivity" />
        <activity android:name=".InspectionActivity" />
        <activity android:name=".InspectionDetailsActivity" />
    </application>
</manifest>